export interface Product {
    id?: string,
    title: string,
    price: string,
    image: string,
    brand?: string,
    description?: string,
    quantity?: string,
    doctor?:string,
    agreement?:string,
    duration?: string,
    service?: string
}